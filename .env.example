# SpeicherWald – Beispielumgebung (.env.example)
# Kopiere diese Datei zu ".env" im Projektroot und passe Werte an.
# Hinweis: Alle Variablen sind optional. Sie überschreiben die Defaults aus config/default.toml.

########################
# Server
########################
#SPEICHERWALD__SERVER__HOST=127.0.0.1
#SPEICHERWALD__SERVER__PORT=8080

########################
# Datenbank (SQLite)
########################
# Standard: sqlite://data/speicherwald.db
#SPEICHERWALD__DATABASE__URL=sqlite://data/speicherwald.db

########################
# Scan-Defaults (nur wirksam, wenn Felder im POST /scans weggelassen werden)
########################
#SPEICHERWALD__SCAN_DEFAULTS__FOLLOW_SYMLINKS=false
#SPEICHERWALD__SCAN_DEFAULTS__INCLUDE_HIDDEN=true
#SPEICHERWALD__SCAN_DEFAULTS__MEASURE_LOGICAL=true
#SPEICHERWALD__SCAN_DEFAULTS__MEASURE_ALLOCATED=true
# JSON-Array von Glob-Mustern
#SPEICHERWALD__SCAN_DEFAULTS__EXCLUDES=["**/target","**/.git","**/node_modules"]
# Max. Tiefe (leer = unbegrenzt)
#SPEICHERWALD__SCAN_DEFAULTS__MAX_DEPTH=
# Worker-Anzahl (leer = auto)
#SPEICHERWALD__SCAN_DEFAULTS__CONCURRENCY=

########################
# Scanner (Leistung/Flush)
########################
#SPEICHERWALD__SCANNER__BATCH_SIZE=4000
#SPEICHERWALD__SCANNER__FLUSH_THRESHOLD=8000
#SPEICHERWALD__SCANNER__FLUSH_INTERVAL_MS=750
#SPEICHERWALD__SCANNER__DIR_CONCURRENCY=12
# Handle-Limit (leer = kein explizites Limit)
#SPEICHERWALD__SCANNER__HANDLE_LIMIT=

########################
# Sonstiges
########################
# Alternative Konfigurationsdatei ohne Erweiterung (.toml wird automatisch ergänzt)
#SPEICHERWALD_CONFIG=C:/cfg/prod

########################
# Rate Limiting (global middleware defaults)
########################
# Maximale Anzahl Requests pro Fenster (pro IP). Default (ohne .env): 1000
#SPEICHERWALD_RATE_LIMIT_MAX_REQUESTS=2000
# Fensterlänge in Sekunden. Default: 60
#SPEICHERWALD_RATE_LIMIT_WINDOW_SECONDS=60

# Hinweis: Endpoint-spezifische Limits (z.B. "/scans", "/scans/:id/search", "/drives")
# werden derzeit im Code gesetzt (siehe src/state.rs). Wenn du willst, bauen wir die
# auch über .env konfigurierbar.
